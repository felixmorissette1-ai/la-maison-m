<header class="lmm-header full-width">
  <div class="lmm-header__inner">
    <div class="lmm-header__cell lmm-header__cell--toggle">
      <button type="button" class="lmm-menu-toggle" aria-expanded="false" aria-controls="lmm-mobile-nav" onclick="toggleMobileNav()">
        <span class="lmm-menu-toggle__bar"></span>
        <span class="lmm-menu-toggle__bar"></span>
        <span class="lmm-menu-toggle__bar"></span>
        <span class="lmm-visually-hidden">Menu</span>
      </button>
    </div>

    <div class="lmm-header__cell lmm-header__cell--nav">
      <a href="/pages/sur-mesure" class="lmm-nav-link lmm-nav-link--cta{% if request.path contains 'sur-mesure' %} lmm-nav-link--active{% endif %}">Configurez votre complet</a>
    </div>
    <div class="lmm-header__cell lmm-header__cell--nav lmm-header__cell--dropdown">
      <a href="/collections/collection" class="lmm-nav-link{% if request.path contains '/collections/collection' or request.path contains '/collections/chemises' %} lmm-nav-link--active{% endif %}">Collection</a>
      <div class="lmm-header__dropdown">
        <a href="/collections/collection" class="lmm-header__dropdown-link">Complets</a>
        <a href="/collections/chemises" class="lmm-header__dropdown-link">Chemises</a>
      </div>
    </div>
    <div class="lmm-header__cell lmm-header__cell--nav">
      {% assign now_ts = 'now' | date: '%s' %}
      {% assign rdv_ts = customer.metafields.custom.rdv_date_heure | date: '%s' %}
      {% if customer and customer.metafields.custom.rdv_futur and rdv_ts > now_ts %}
        <a href="#" class="lmm-nav-link" onclick="event.preventDefault(); ouvrirGestionModal()">Gestion du rendez-vous</a>
      {% else %}
        <a href="#" class="lmm-nav-link" onclick="event.preventDefault(); ouvrirLocalisationModal()">Prendre rendez-vous</a>
      {% endif %}
    </div>

    <div class="lmm-header__cell lmm-header__cell--logo">
      <a href="{{ routes.root_url }}" class="lmm-header__logo">
        {% if section.settings.logo_image %}
          <img src="{{ section.settings.logo_image | image_url: width: 400 }}" alt="{{ shop.name }}" class="lmm-header__logo-img" style="height: {{ section.settings.logo_height | default: 32 }}px;">
        {% else %}
          <span class="lmm-logo-main">La Maison M.</span>
        {% endif %}
        <span class="lmm-logo-sub">Tailleur sur mesure</span>
      </a>
    </div>

    <div class="lmm-header__cell lmm-header__cell--nav lmm-header__cell--dropdown">
      <a href="/pages/qui-nous-sommes" class="lmm-nav-link{% if request.path contains 'qui-nous-sommes' or request.path contains 'comment-ca-fonctionne' %} lmm-nav-link--active{% endif %}">À propos</a>
      <div class="lmm-header__dropdown">
        <a href="/pages/qui-nous-sommes" class="lmm-header__dropdown-link">Qui nous sommes</a>
        <a href="/pages/comment-ca-fonctionne" class="lmm-header__dropdown-link">Comment ça fonctionne</a>
      </div>
    </div>
    <div class="lmm-header__cell lmm-header__cell--nav">
      <a href="/pages/aide-configuration" class="lmm-nav-link{% if request.path contains 'aide-configuration' %} lmm-nav-link--active{% endif %}">Conseils stylistiques</a>
    </div>

    <div class="lmm-header__cell lmm-header__cell--icons">
      <div class="lmm-header__icons">
        <a href="{{ routes.cart_url }}" class="lmm-icon-btn lmm-cart-btn">
          {{ 'icon-cart.svg' | inline_asset_content }}
          {% if cart.item_count > 0 %}
            {% comment %}
              Pastille unie plutôt qu'un chiffre : cart.item_count additionne
              les quantités de toutes les lignes, mais le configurateur
              sur-mesure encode son prix dans la quantité (voir sm-checkout,
              couturiere-actions.js) — un seul complet peut y afficher une
              quantité de plusieurs centaines de milliers. Un chiffre exact
              n'a donc plus de sens ici ; "le panier n'est pas vide" suffit.
            {% endcomment %}
            <span class="lmm-cart-count" aria-hidden="true"></span>
            <span class="lmm-visually-hidden">Panier non vide</span>
          {% endif %}
        </a>

        {% if shop.customer_accounts_enabled %}
          <shopify-account menu="{{ section.settings.customer_account_menu }}" class="lmm-icon-btn">
            {{ 'icon-account.svg' | inline_asset_content }}
          </shopify-account>
        {% endif %}
      </div>
    </div>
  </div>
</header>

<div id="lmm-mobile-nav-backdrop" class="lmm-mobile-nav__backdrop" onclick="fermerMobileNav()"></div>
<nav id="lmm-mobile-nav" class="lmm-mobile-nav" aria-hidden="true">
  <a href="/pages/sur-mesure" class="lmm-mobile-nav__link lmm-mobile-nav__link--cta{% if request.path contains 'sur-mesure' %} lmm-mobile-nav__link--active{% endif %}">Configurez votre complet</a>
  <a href="/collections/collection" class="lmm-mobile-nav__link{% if request.path contains '/collections/collection' %} lmm-mobile-nav__link--active{% endif %}">Complets</a>
  <a href="/collections/chemises" class="lmm-mobile-nav__link{% if request.path contains '/collections/chemises' %} lmm-mobile-nav__link--active{% endif %}">Chemises</a>
  {% if customer and customer.metafields.custom.rdv_futur and rdv_ts > now_ts %}
    <a href="#" class="lmm-mobile-nav__link" onclick="event.preventDefault(); fermerMobileNav(); ouvrirGestionModal()">Gestion du rendez-vous</a>
  {% else %}
    <a href="#" class="lmm-mobile-nav__link" onclick="event.preventDefault(); fermerMobileNav(); ouvrirLocalisationModal()">Prendre rendez-vous</a>
  {% endif %}
  <a href="/pages/qui-nous-sommes" class="lmm-mobile-nav__link{% if request.path contains 'qui-nous-sommes' %} lmm-mobile-nav__link--active{% endif %}">Qui nous sommes</a>
  <a href="/pages/comment-ca-fonctionne" class="lmm-mobile-nav__link{% if request.path contains 'comment-ca-fonctionne' %} lmm-mobile-nav__link--active{% endif %}">Comment ça fonctionne</a>
  <a href="/pages/aide-configuration" class="lmm-mobile-nav__link{% if request.path contains 'aide-configuration' %} lmm-mobile-nav__link--active{% endif %}">Conseils stylistiques</a>
</nav>

<div class="lmm-cal-modal" id="lmm-localisation-modal" onclick="if(event.target===this) fermerLocalisationModal()">
  <div class="lmm-cal-modal__inner" style="padding:40px 32px; max-width:600px; width:90vw; max-height:90vh; display:flex; flex-direction:column;">
    <button class="lmm-cal-modal__close" onclick="fermerLocalisationModal()">×</button>
    <h2 style="font-family:Georgia,serif; font-size:22px; font-weight:400; color:var(--lmm-text); margin:0 0 8px;">Votre tailleur</h2>
    <p style="font-size:12px; color:var(--lmm-muted); letter-spacing:0.04em; margin:0 0 20px; line-height:1.7;">Nous trouvons le tailleur le plus près de vous.</p>

    <div id="lmm-loc-type" style="display:none; flex-direction:column; gap:10px; margin-bottom:20px;">
      <button onclick="choisirTypeRdv('mensurations')" style="background:var(--lmm-ivoire-deep); color:var(--lmm-text); border:none; border-radius:8px; padding:16px 20px; text-align:left; cursor:pointer; font-family:inherit;">
        <span style="display:block; font-size:13px; letter-spacing:0.02em;">Rendez-vous mensurations</span>
        <span style="display:block; font-size:11px; font-weight:300; opacity:0.8; margin-top:4px;">Prise de vos mesures et retouches.</span>
      </button>
      <button onclick="choisirTypeRdv('tissus')" style="background:var(--lmm-ivoire-deep); color:var(--lmm-text); border:none; border-radius:8px; padding:16px 20px; text-align:left; cursor:pointer; font-family:inherit;">
        <span style="display:block; font-size:13px; letter-spacing:0.02em;">Voir les tissus et conseils vestimentaires</span>
        <span style="display:block; font-size:11px; font-weight:300; opacity:0.8; margin-top:4px;">Touchez les tissus et soyez conseillé avant de commander.</span>
      </button>
    </div>

    <div id="lmm-loc-compte-requis" style="display:none; flex-direction:column; gap:16px; margin-bottom:20px;">
      <div>
        <p style="font-family:Georgia,serif; font-size:16px; color:var(--lmm-text); margin:0 0 8px;">Un compte est requis pour ce type de rendez-vous</p>
        <p style="font-size:12px; color:var(--lmm-muted); line-height:1.7; margin:0;">Connectez-vous ou créez un compte pour réserver un rendez-vous « Voir les tissus et conseils vestimentaires ».</p>
      </div>
      <div style="display:flex; flex-direction:column; gap:8px;">
        <a href="/account/login" target="_blank" rel="noopener" style="display:flex; align-items:center; justify-content:center; background:var(--lmm-bordeaux); color:var(--lmm-ivoire-pale); border-radius:8px; padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; text-decoration:none; font-family:inherit;">
          Se connecter et prendre rendez-vous
        </a>
        <a href="/account/register" target="_blank" rel="noopener" style="text-align:center; font-size:11px; letter-spacing:0.08em; color:var(--lmm-muted); text-decoration:none; padding:6px 0;">
          Créer un compte
        </a>
      </div>
      <div style="border-top:1px solid var(--lmm-border); padding-top:16px;">
        <p style="font-size:11px; color:var(--lmm-muted); line-height:1.7; margin:0;">Vous préférez ne pas créer de compte ? Vous pouvez toujours vous présenter directement en succursale, sans rendez-vous, aux heures d'ouverture habituelles de nos couturières :</p>
        <p style="font-size:12px; color:var(--lmm-text); line-height:1.7; margin:8px 0 0; white-space:pre-line;">{{ section.settings.horaire_couturieres }}</p>
      </div>
      <button onclick="retourChoixTypeRdv()" style="background:none; border:none; color:var(--lmm-muted); font-size:11px; letter-spacing:0.06em; text-transform:uppercase; cursor:pointer; font-family:inherit; align-self:center;">
        Retour
      </button>
    </div>

    <div id="lmm-loc-auto" style="display:flex; flex-direction:column; gap:10px; margin-bottom:20px;">
      <button onclick="localiserAuto()" style="background:var(--lmm-ivoire-deep); color:var(--lmm-text); border:none; border-radius:8px; padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; cursor:pointer; font-family:inherit; display:flex; align-items:center; justify-content:center;">
        Utiliser ma localisation
      </button>
      <p style="text-align:center; font-size:11px; color:var(--lmm-border); margin:0;">— ou —</p>
      <div style="display:flex; gap:8px;">
        <input id="lmm-code-postal" type="text" placeholder="Code postal (ex: G6V 1A1)" style="flex:1; border:1px solid var(--lmm-border); padding:12px; font-family:inherit; font-size:13px; color:var(--lmm-text);">
        <button onclick="localiserParCodePostal()" style="background:none; color:var(--lmm-text); border:1.5px solid var(--lmm-text); border-radius:8px; padding:12px 20px; font-size:11px; letter-spacing:0.08em; text-transform:uppercase; cursor:pointer; font-family:inherit;">
          OK
        </button>
      </div>
    </div>

    <div id="lmm-loc-loading" style="display:none; text-align:center; padding:20px 0;">
      <p style="font-size:12px; color:var(--lmm-muted); letter-spacing:0.08em;">Recherche en cours...</p>
    </div>

    <div id="lmm-loc-resultat" style="display:none; flex-direction:column; gap:0; flex:1; min-height:0;">
      <div id="lmm-map" style="width:100%; height:200px; background:var(--lmm-ivoire-pale); flex-shrink:0;"></div>

      <div id="lmm-vendeuse-legende" style="display:none; flex-shrink:0; border:1px solid var(--lmm-border); border-top:none; padding:16px 18px; flex-direction:column; gap:12px; background:var(--lmm-ivoire-pale);">
        <div style="display:flex; flex-direction:column; gap:10px;">
          <span style="display:flex; align-items:center; gap:10px; font-size:12px; color:var(--lmm-text);">
            <svg width="18" height="18" viewBox="0 0 18 18" style="flex-shrink:0;"><circle cx="9" cy="9" r="8" fill="var(--lmm-bordeaux)" stroke="var(--lmm-text)" stroke-width="1"/></svg>
            Mensurations, altérations et visualisation des tissus
          </span>
          <span style="display:flex; align-items:center; gap:10px; font-size:12px; color:var(--lmm-text);">
            <svg width="18" height="18" viewBox="0 0 18 18" style="flex-shrink:0;"><circle cx="9" cy="9" r="5" fill="var(--lmm-ivoire)" stroke="var(--lmm-text)" stroke-width="1"/></svg>
            Mensurations et altérations
          </span>
        </div>
        <button type="button" id="lmm-vendeuse-toggle" onclick="toggleVendeuseSeulement()" style="display:flex; align-items:center; justify-content:space-between; gap:8px; background:none; border:none; border-top:1px solid var(--lmm-border); padding:12px 0 0; cursor:pointer; font-family:inherit; width:100%;">
          <span style="font-size:11px; letter-spacing:0.04em; color:var(--lmm-text);">Voir seulement les tailleurs avec livres de tissus</span>
          <span class="lmm-toggle-track"><span class="lmm-toggle-thumb"></span></span>
        </button>
      </div>

      <div id="lmm-couturieres-liste" style="display:flex; flex-direction:column; gap:8px; overflow-y:auto; flex:1; min-height:0; margin-top:12px;"></div>

      <div style="flex-shrink:0; display:flex; flex-direction:column; gap:8px; padding-top:4px;">
        <button onclick="confirmerCouturiereEtOuvrirCal()" style="background:var(--lmm-ivoire-deep); color:var(--lmm-text); border:none; border-radius:8px; padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; cursor:pointer; font-family:inherit;">
          Prendre rendez-vous
        </button>
        <button onclick="afficherLocAuto()" style="background:none; border:1px solid var(--lmm-border); border-radius:8px; color:var(--lmm-muted); padding:12px; font-size:11px; letter-spacing:0.08em; text-transform:uppercase; cursor:pointer; font-family:inherit;">
          Changer de localisation
        </button>
      </div>
    </div>

    <p id="lmm-loc-erreur" style="color:var(--lmm-error); font-size:12px; margin:12px 0 0; display:none;">Impossible de trouver votre localisation. Essayez avec un code postal.</p>
  </div>
</div>

<div class="lmm-cal-modal" id="lmm-annulation-modal" onclick="if(event.target===this) fermerAnnulationModal()">
  <div class="lmm-cal-modal__inner" style="padding:40px 32px; max-width:480px;">
    <button class="lmm-cal-modal__close" onclick="fermerAnnulationModal()">×</button>
    <h2 style="font-family:Georgia,serif; font-size:22px; font-weight:400; color:var(--lmm-text); margin:0 0 8px;">Annuler le rendez-vous</h2>
    <p style="font-size:12px; color:var(--lmm-muted); letter-spacing:0.04em; margin:0 0 24px;">Veuillez indiquer la raison de votre annulation.</p>
    <textarea id="lmm-annulation-raison" placeholder="Raison de l'annulation..." style="width:100%; height:100px; border:1px solid var(--lmm-border); padding:12px; font-family:inherit; font-size:13px; color:var(--lmm-text); resize:none; box-sizing:border-box; margin-bottom:16px;"></textarea>
    <div style="display:flex; flex-direction:column; gap:10px;">
      <button onclick="confirmerAnnulation()" style="background:var(--lmm-ivoire-deep); color:var(--lmm-text); border:none; border-radius:8px; padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; cursor:pointer; font-family:inherit;">
        Confirmer l'annulation
      </button>
      <button onclick="fermerAnnulationModal()" style="background:none; border:1px solid var(--lmm-border); border-radius:8px; color:var(--lmm-muted); padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; cursor:pointer; font-family:inherit;">
        Retour
      </button>
    </div>
    <p id="lmm-annulation-erreur" style="color:var(--lmm-error); font-size:12px; margin:12px 0 0; display:none;">Une erreur est survenue. Veuillez réessayer.</p>
  </div>
</div>

<div class="lmm-cal-modal" id="lmm-signup-modal">
  <div class="lmm-cal-modal__inner" style="padding:40px 32px; max-width:480px;">
    <h2 style="font-family:Georgia,serif; font-size:22px; font-weight:400; color:var(--lmm-text); margin:0 0 8px;">Rendez-vous confirmé ✓</h2>
    <p style="font-size:13px; color:var(--lmm-muted); margin:0 0 24px; line-height:1.6;">Connectez-vous pour accéder au paiement et que vos mensurations soient enregistrées à votre nom.</p>
    <a id="lmm-signup-link" href="/account/login" target="_blank" rel="noopener" style="display:flex; align-items:center; justify-content:center; background:var(--lmm-bordeaux); color:var(--lmm-ivoire-pale); border-radius:8px; padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; text-decoration:none; font-family:inherit; margin-bottom:10px;">
      Se connecter
    </a>
    <a href="/account/register" target="_blank" rel="noopener" style="display:block; text-align:center; font-size:11px; letter-spacing:0.08em; color:var(--lmm-muted); text-decoration:none; margin-bottom:16px;">
      Créer un compte
    </a>
    <input type="hidden" id="lmm-signup-email" value="">
    <button onclick="document.getElementById('lmm-signup-modal').classList.remove('open'); window.location.href='/cart';" style="background:none; border:none; color:var(--lmm-border); font-size:11px; letter-spacing:0.08em; text-transform:uppercase; cursor:pointer; font-family:inherit; width:100%; padding:10px;">
      Continuer sans compte
    </button>
  </div>
</div>

<div id="lmm-booking-overlay" style="display:none; position:fixed; inset:0; background:var(--lmm-ivoire-deep); z-index:99999; flex-direction:column; align-items:center; justify-content:center; gap:20px;">
  <p style="font-family:Georgia,serif; font-size:22px; color:var(--lmm-text); letter-spacing:0.04em;">Rendez-vous confirmé</p>
  <p style="font-size:12px; color:var(--lmm-bordeaux); letter-spacing:0.12em; text-transform:uppercase;">Redirection vers votre panier...</p>
  <div style="width:40px; height:2px; background:var(--lmm-bordeaux); animation:lmm-progress 3s linear forwards;"></div>
</div>

<div class="lmm-cal-modal" id="lmm-reschedule-modal" onclick="if(event.target===this) fermerRescheduleModal()">
  <div class="lmm-cal-modal__inner">
    <button class="lmm-cal-modal__close" onclick="fermerRescheduleModal()">×</button>
    <iframe id="lmm-reschedule-iframe" src="" frameborder="0" allow="camera; microphone; fullscreen" style="width:100%; min-height:600px; display:block;"></iframe>
  </div>
</div>

<div class="lmm-cal-modal" id="lmm-cal-modal" onclick="if(event.target===this) fermerCalModal()">
  <div class="lmm-cal-modal__inner">
    <button class="lmm-cal-modal__close" onclick="fermerCalModal()">×</button>
    <button onclick="retourSelectionCouturiere()" style="position:absolute; top:16px; left:16px; z-index:2; background:none; border:none; color:var(--lmm-muted); font-size:11px; letter-spacing:0.06em; text-transform:uppercase; cursor:pointer; font-family:inherit;">
      Changer de tailleur
    </button>
    <iframe id="lmm-cal-iframe" src="" frameborder="0" allow="camera; microphone; fullscreen" style="width:100%; min-height:600px; display:block;"></iframe>
  </div>
</div>

<div class="lmm-cal-modal" id="lmm-gestion-modal" onclick="if(event.target===this) fermerGestionModal()">
  <div class="lmm-cal-modal__inner" style="padding:40px 32px; max-width:480px;">
    <button class="lmm-cal-modal__close" onclick="fermerGestionModal()">×</button>
    <h2 style="font-family:Georgia,serif; font-size:22px; font-weight:400; color:var(--lmm-text); margin:0 0 24px;">Votre rendez-vous</h2>

    {% if customer and customer.metafields.custom.rdv_date_heure != blank %}
      <div style="background:var(--lmm-ivoire-deep); padding:20px 24px; margin-bottom:28px;">
        <p style="font-size:11px; color:var(--lmm-muted); letter-spacing:0.1em; text-transform:uppercase; margin:0 0 6px;">Date et heure</p>
        <p style="font-family:Georgia,serif; font-size:18px; color:var(--lmm-text); margin:0;">
          {{ customer.metafields.custom.rdv_date_heure | date: "%A %d %B %Y" | capitalize }}
        </p>
        <p style="font-size:14px; color:var(--lmm-muted); margin:4px 0 0;">
          {{ customer.metafields.custom.rdv_date_heure | date: "%H:%M" }}
        </p>
      </div>
    {% endif %}

    <div style="display:flex; flex-direction:column; gap:10px;">
      {% if customer and customer.metafields.custom.rdv_uid != blank %}
        <button onclick="ouvrirRescheduleModal()" style="display:flex; align-items:center; justify-content:center; background:var(--lmm-ivoire-deep); color:var(--lmm-text); border:none; border-radius:8px; padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; cursor:pointer; font-family:inherit; width:100%;">
          Modifier le rendez-vous
        </button>
        <button onclick="ouvrirChangementCouturiere()" style="display:flex; align-items:center; justify-content:center; background:none; border:1px solid var(--lmm-border); border-radius:8px; color:var(--lmm-muted); padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; cursor:pointer; font-family:inherit; width:100%;">
          Changer de tailleur
        </button>
        <button onclick="ouvrirAnnulationModal()" style="display:flex; align-items:center; justify-content:center; background:none; border:1px solid var(--lmm-border); border-radius:8px; color:var(--lmm-muted); padding:14px 32px; font-size:11px; letter-spacing:0.12em; text-transform:uppercase; cursor:pointer; font-family:inherit; width:100%;">
          Annuler le rendez-vous
        </button>
      {% endif %}
    </div>
  </div>
</div>

<script>
  // === Retour forcé vers le formulaire de conseils stylistiques ===
  // Le return_url passé à /account/login n'est pas fiable pour la
  // CRÉATION de compte sur le nouveau système de comptes Shopify
  // (hébergé sur un domaine externe) — Shopify peut ramener le client
  // n'importe où sur le shop plutôt que sur la page d'où il est parti,
  // ce qui faisait perdre sa demande déjà remplie. Comme header.liquid
  // se charge sur TOUTES les pages, on vérifie ici, dès qu'on arrive
  // n'importe où sur le site connecté avec une demande en attente
  // (déposée uniquement par le bouton "Se connecter et envoyer ma
  // demande" — voir aide-configuration.liquid), et on redirige de force
  // vers la page du formulaire, qui se charge alors de la remplir et de
  // l'envoyer automatiquement.
  {% if customer %}
    (function () {
      var pending;
      try {
        pending = localStorage.getItem('lmm_aide_config_pending');
      } catch (e) {
        pending = null;
      }
      if (pending && window.location.pathname.indexOf('aide-configuration') === -1) {
        window.location.href = '/pages/aide-configuration';
      }
    })();
  {% endif %}

  // === Menu mobile ===
  function toggleMobileNav() {
    var nav = document.getElementById('lmm-mobile-nav');
    if (nav.classList.contains('open')) {
      fermerMobileNav();
    } else {
      ouvrirMobileNav();
    }
  }

  function ouvrirMobileNav() {
    document.getElementById('lmm-mobile-nav').classList.add('open');
    document.getElementById('lmm-mobile-nav').setAttribute('aria-hidden', 'false');
    document.getElementById('lmm-mobile-nav-backdrop').classList.add('open');
    document.querySelector('.lmm-menu-toggle').setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }

  function fermerMobileNav() {
    document.getElementById('lmm-mobile-nav').classList.remove('open');
    document.getElementById('lmm-mobile-nav').setAttribute('aria-hidden', 'true');
    document.getElementById('lmm-mobile-nav-backdrop').classList.remove('open');
    document.querySelector('.lmm-menu-toggle').setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }

  window.addEventListener('resize', function () {
    if (window.innerWidth > 900) fermerMobileNav();
  });

  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') fermerMobileNav();
  });
  // === Fin menu mobile ===

  // === Localisation / Tailleur ===
  var tunnelUrl = 'https://lmm-backend.vercel.app';
  var lmmRdvType = null;

  // Localisation demandée une seule fois par session, dès l'arrivée sur le
  // site (pas seulement quand le modal de rendez-vous s'ouvre) — pour que le
  // choix du client soit déjà en cache et qu'on n'ait pas à le lui redemander
  // au moment de prendre rendez-vous. Le code postal reste toujours
  // disponible en alternative/override via "Changer de localisation".
  (function () {
    if (sessionStorage.getItem('lmm_geo_asked')) return;
    if (!navigator.geolocation) return;
    navigator.geolocation.getCurrentPosition(
      function (pos) {
        sessionStorage.setItem('lmm_geo_asked', '1');
        lmmEcrireGeoCache(pos.coords.latitude, pos.coords.longitude);
      },
      function () {
        sessionStorage.setItem('lmm_geo_asked', '1');
      },
      { timeout: 8000 }
    );
  })();

  function lmmLireGeoCache() {
    try {
      var raw = sessionStorage.getItem('lmm_geo');
      return raw ? JSON.parse(raw) : null;
    } catch (e) {
      return null;
    }
  }

  function lmmEcrireGeoCache(lat, lng) {
    try {
      sessionStorage.setItem('lmm_geo', JSON.stringify({ lat: lat, lng: lng }));
    } catch (e) {}
  }

  function ouvrirLocalisationModal(type) {
    document.getElementById('lmm-localisation-modal').classList.add('open');
    if (type) {
      choisirTypeRdv(type);
    } else {
      lmmRdvType = null;
      document.getElementById('lmm-loc-type').style.display = 'flex';
      document.getElementById('lmm-loc-compte-requis').style.display = 'none';
      document.getElementById('lmm-loc-auto').style.display = 'none';
      document.getElementById('lmm-loc-loading').style.display = 'none';
      document.getElementById('lmm-loc-resultat').style.display = 'none';
      document.getElementById('lmm-loc-erreur').style.display = 'none';
    }
  }

  function choisirTypeRdv(type) {
    // "Voir les tissus" nécessite un compte — même logique que le
    // paiement au panier (gate "Se connecter pour continuer") : on ne
    // laisse pas continuer sans compte, mais on offre toujours l'option
    // de se présenter directement en succursale sans rendez-vous.
    if (type === 'tissus') {
      {% if customer %}
      {% else %}
      document.getElementById('lmm-loc-type').style.display = 'none';
      document.getElementById('lmm-loc-compte-requis').style.display = 'flex';
      return;
      {% endif %}
    }
    lmmRdvType = type;
    document.getElementById('lmm-loc-type').style.display = 'none';
    demarrerRecherche();
  }

  function retourChoixTypeRdv() {
    document.getElementById('lmm-loc-compte-requis').style.display = 'none';
    document.getElementById('lmm-loc-type').style.display = 'flex';
  }

  // Si on a déjà la localisation en cache (voir IIFE plus haut), on saute
  // directement à la recherche — sinon on retombe sur l'écran habituel
  // (bouton localisation + code postal).
  function demarrerRecherche() {
    var cache = lmmLireGeoCache();
    if (cache) {
      lmmOrigine = cache;
      afficherLoading();
      rechercherParCoords(cache.lat, cache.lng);
    } else {
      afficherLocAuto();
    }
  }

  function fermerLocalisationModal() {
    document.getElementById('lmm-localisation-modal').classList.remove('open');
    document.getElementById('lmm-loc-erreur').style.display = 'none';
  }

  function afficherLocAuto() {
    document.getElementById('lmm-loc-type').style.display = 'none';
    document.getElementById('lmm-loc-auto').style.display = 'flex';
    document.getElementById('lmm-loc-loading').style.display = 'none';
    document.getElementById('lmm-loc-resultat').style.display = 'none';
    document.getElementById('lmm-code-postal').value = '';
    document.getElementById('lmm-loc-erreur').style.display = 'none';
    lmmMap = null;
    document.getElementById('lmm-map').innerHTML = '';
    document.getElementById('lmm-couturieres-liste').innerHTML = '';
  }

  function afficherLoading() {
    document.getElementById('lmm-loc-auto').style.display = 'none';
    document.getElementById('lmm-loc-loading').style.display = 'block';
    document.getElementById('lmm-loc-resultat').style.display = 'none';
    document.getElementById('lmm-loc-erreur').style.display = 'none';
  }

  var lmmLienCalChoisi = '';
  var lmmMap = null;
  var lmmMapCharge = false;
  var lmmVendeuseSeulement = false;
  var lmmListeActive = [];
  var lmmOrigine = null;

  // L'API Google Maps ne comprend pas var(--x) — ce ne sont pas de vrais
  // éléments DOM stylés — donc on résout la valeur RÉELLE de la variable
  // CSS ici, au moment de l'appel, plutôt que de recopier des hex figés
  // qui se désynchroniseraient si la palette change dans les réglages du
  // thème (c'est exactement ce que faisait l'ancienne version).
  function lmmCssVar(name) {
    return getComputedStyle(document.documentElement).getPropertyValue(name).trim();
  }

  function chargerGoogleMaps(callback) {
    if (window.google && window.google.maps) { callback(); return; }
    window._lmmMapCallback = callback;
    var s = document.createElement('script');
    s.src = 'https://maps.googleapis.com/maps/api/js?key=AIzaSyCyzVI3XFE-f8ir2aE4QTjUB0haaJRyhig&callback=_lmmMapCallback';
    s.async = true;
    document.head.appendChild(s);
  }

  function afficherResultat(couturieres) {
    lmmDernierResultat = couturieres;
    lmmVendeuseSeulement = false;
    document.getElementById('lmm-loc-loading').style.display = 'none';
    document.getElementById('lmm-loc-resultat').style.display = 'flex';
    rendererResultat();
  }

  function toggleVendeuseSeulement() {
    lmmVendeuseSeulement = !lmmVendeuseSeulement;
    rendererResultat();
  }

  function rendererResultat() {
    // En mode "voir les tissus", le filtre est forcé plutôt que laissé au
    // choix du client — c'est justement le type de rendez-vous qu'il a
    // sélectionné, le bouton-bascule devient redondant et est caché.
    var forceTissus = lmmRdvType === 'tissus';
    var hasVendeuse = lmmDernierResultat.some(function(c) { return c.vendeuse; });
    var legende = document.getElementById('lmm-vendeuse-legende');

    if (forceTissus) {
      legende.style.display = 'none';
    } else {
      legende.style.display = hasVendeuse ? 'flex' : 'none';
      if (!hasVendeuse) lmmVendeuseSeulement = false;
      document.getElementById('lmm-vendeuse-toggle').classList.toggle('active', lmmVendeuseSeulement);
    }

    var couturieres = (forceTissus || lmmVendeuseSeulement)
      ? lmmDernierResultat.filter(function(c) { return c.vendeuse; })
      : lmmDernierResultat;
    lmmListeActive = couturieres;

    var liste = document.getElementById('lmm-couturieres-liste');
    liste.innerHTML = '';

    if (!couturieres.length) {
      liste.innerHTML = '<p style="font-size:12px; color:var(--lmm-muted); text-align:center; padding:24px 0;">' +
        (forceTissus ? 'Aucun tailleur avec présentation de tissus dans votre secteur pour l\'instant.' : 'Aucun tailleur dans votre secteur pour l\'instant.') +
        '</p>';
      if (lmmMap) { document.getElementById('lmm-map').innerHTML = ''; lmmMap = null; }
      return;
    }

    lmmLienCalChoisi = couturieres[0].lien_cal || '';

    couturieres.forEach(function(c, i) {
      var card = document.createElement('div');
      card.id = 'lmm-card-' + i;
      card.setAttribute('onclick', 'selectionnerCouturiere(' + i + ')');
      card.style.cssText = 'background:var(--lmm-ivoire-pale); border:2px solid var(--lmm-border); padding:14px 18px; cursor:pointer; display:flex; justify-content:space-between; align-items:center; transition:background 0.15s, border-color 0.15s;';
      var typeService = c.vendeuse
        ? '<span style="display:flex; align-items:center; gap:6px; font-size:10px; letter-spacing:0.03em; color:var(--lmm-bordeaux); margin-top:4px;"><svg width="10" height="10" viewBox="0 0 10 10" style="flex-shrink:0;"><circle cx="5" cy="5" r="4" fill="var(--lmm-bordeaux)" stroke="var(--lmm-text)" stroke-width="1"/></svg>Mensurations, altérations et visualisation des tissus</span>'
        : '<span style="display:flex; align-items:center; gap:6px; font-size:10px; letter-spacing:0.03em; color:var(--lmm-muted); margin-top:4px;"><svg width="9" height="9" viewBox="0 0 9 9" style="flex-shrink:0;"><circle cx="4.5" cy="4.5" r="3.5" fill="var(--lmm-ivoire)" stroke="var(--lmm-text)" stroke-width="1"/></svg>Mensurations et altérations</span>';
      card.innerHTML = '<div style="flex:1;">' +
        (i === 0 ? '<p style="font-size:10px;color:var(--lmm-bordeaux);letter-spacing:0.1em;text-transform:uppercase;margin:0 0 4px;">La plus proche</p>' : '') +
        '<p style="font-family:Georgia,serif;font-size:15px;color:var(--lmm-text);margin:0 0 2px;">' + c.nom + '</p>' +
        '<p style="font-size:11px;color:var(--lmm-muted);margin:0;">' + c.adresse + '</p>' +
        typeService +
        '</div>' +
        '<p id="lmm-btn-' + i + '" style="font-size:12px;color:var(--lmm-bordeaux);margin:0;flex-shrink:0;margin-left:12px;">' + c.distance + '</p>';
      liste.appendChild(card);
    });
    selectionnerCouturiere(0);

    // Map — centrée sur la position du client (pas sur les tailleurs), avec un
    // zoom serré sur sa région pour éviter de dézoomer jusqu'à voir tout le Québec
    // dès qu'un tailleur plus lointain fait partie des résultats.
    chargerGoogleMaps(function() {
      var mapEl = document.getElementById('lmm-map');
      mapEl.innerHTML = '';
      var center = lmmOrigine || { lat: couturieres[0].lat, lng: couturieres[0].lng };
      lmmMap = new google.maps.Map(mapEl, {
        center: center,
        zoom: 11,
        disableDefaultUI: true,
        zoomControl: true,
        styles: [
          { elementType: 'geometry', stylers: [{ color: lmmCssVar('--lmm-ivoire-pale') }] },
          { elementType: 'labels.icon', stylers: [{ visibility: 'off' }] },
          { elementType: 'labels.text.fill', stylers: [{ color: lmmCssVar('--lmm-muted') }] },
          { elementType: 'labels.text.stroke', stylers: [{ color: lmmCssVar('--lmm-white') }] },
          { featureType: 'water', elementType: 'geometry', stylers: [{ color: lmmCssVar('--lmm-ivoire-line') }] },
          { featureType: 'road', elementType: 'geometry.fill', stylers: [{ color: lmmCssVar('--lmm-white') }] },
          { featureType: 'road', elementType: 'geometry.stroke', stylers: [{ color: lmmCssVar('--lmm-border') }] },
          { featureType: 'poi', stylers: [{ visibility: 'off' }] },
          { featureType: 'transit', stylers: [{ visibility: 'off' }] },
          { featureType: 'administrative', elementType: 'geometry', stylers: [{ color: lmmCssVar('--lmm-border') }] }
        ]
      });
      if (lmmOrigine) {
        new google.maps.Marker({
          position: lmmOrigine,
          map: lmmMap,
          title: 'Vous',
          icon: {
            path: google.maps.SymbolPath.CIRCLE,
            scale: 6,
            fillColor: '#4285F4',
            fillOpacity: 1,
            strokeColor: '#ffffff',
            strokeWeight: 2
          }
        });
      }
      couturieres.forEach(function(c, i) {
        var pos = { lat: c.lat, lng: c.lng };
        var isNearest = i === 0;
        // Le format des tailleurs "tissus" est fixe (pas de variation selon la
        // proximité ou le filtre actif) pour rester cohérent d'une vue à l'autre.
        var icon = c.vendeuse
          ? {
              path: google.maps.SymbolPath.CIRCLE,
              scale: 11,
              fillColor: lmmCssVar('--lmm-bordeaux'),
              fillOpacity: 1,
              strokeColor: lmmCssVar('--lmm-text'),
              strokeWeight: 2
            }
          : {
              // Bordeaux réservé exclusivement aux tailleurs "tissus" (voir légende).
              // La proximité reste indiquée par la taille et par le libellé dans la liste.
              path: google.maps.SymbolPath.CIRCLE,
              scale: isNearest ? 10 : 7,
              fillColor: lmmCssVar('--lmm-ivoire-deep'),
              fillOpacity: 1,
              strokeColor: lmmCssVar('--lmm-text'),
              strokeWeight: 2
            };
        new google.maps.Marker({
          position: pos,
          map: lmmMap,
          title: c.nom + (c.vendeuse ? ' — présente les tissus' : ''),
          icon: icon
        });
      });
    });
  }

  var lmmDernierResultat = [];
  var lmmIndexChoisi = 0;

  function selectionnerCouturiere(index) {
    lmmIndexChoisi = index;
    lmmLienCalChoisi = lmmListeActive[index].lien_cal || '';
    var n = lmmListeActive.length;
    for (var j = 0; j < n; j++) {
      var card = document.getElementById('lmm-card-' + j);
      var btn = document.getElementById('lmm-btn-' + j);
      if (!card || !btn) continue;
      if (j === index) {
        card.style.background = 'var(--lmm-ivoire-deep)';
        card.style.borderColor = 'var(--lmm-bordeaux)';
        btn.style.color = 'var(--lmm-bordeaux)';
      } else {
        card.style.background = 'var(--lmm-ivoire-pale)';
        card.style.borderColor = 'var(--lmm-border)';
        btn.style.color = 'var(--lmm-muted)';
      }
    }
  }

  function rechercherParCoords(lat, lng) {
    fetch(tunnelUrl + '/api/couturiere', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ lat: lat, lng: lng })
    })
    .then(function(r) { return r.json(); })
    .then(function(data) {
      if (data.success) {
        lmmOrigine = { lat: lat, lng: lng };
        lmmDernierResultat = data.couturieres;
        afficherResultat(data.couturieres);
      } else {
        afficherLocAuto();
        document.getElementById('lmm-loc-erreur').style.display = 'block';
      }
    })
    .catch(function() {
      afficherLocAuto();
      document.getElementById('lmm-loc-erreur').style.display = 'block';
    });
  }

  function localiserAuto() {
    if (!navigator.geolocation) {
      document.getElementById('lmm-loc-erreur').style.display = 'block';
      return;
    }
    afficherLoading();
    navigator.geolocation.getCurrentPosition(
      function(pos) {
        lmmEcrireGeoCache(pos.coords.latitude, pos.coords.longitude);
        rechercherParCoords(pos.coords.latitude, pos.coords.longitude);
      },
      function() {
        afficherLocAuto();
        document.getElementById('lmm-loc-erreur').style.display = 'block';
      }
    );
  }

  function localiserParCodePostal() {
    var cp = document.getElementById('lmm-code-postal').value.trim();
    if (!cp) return;
    afficherLoading();
    fetch(tunnelUrl + '/api/couturiere', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ codePostal: cp })
    })
    .then(function(r) { return r.json(); })
    .then(function(data) {
      if (data.success) {
        lmmOrigine = data.origine || null;
        lmmDernierResultat = data.couturieres;
        afficherResultat(data.couturieres);
      } else {
        afficherLocAuto();
        document.getElementById('lmm-loc-erreur').textContent = data.error || 'Code postal invalide.';
        document.getElementById('lmm-loc-erreur').style.display = 'block';
      }
    })
    .catch(function() {
      afficherLocAuto();
      document.getElementById('lmm-loc-erreur').style.display = 'block';
    });
  }

  function confirmerCouturiereEtOuvrirCal() {
    fermerLocalisationModal();
    calCharge = false;
    ouvrirCalModal(lmmLienCalChoisi);
  }
  // === Fin Localisation ===

  var calCharge = false;
  function ouvrirCalModal(lienCal) {
    document.getElementById('lmm-cal-modal').classList.add('open');
    if (!calCharge) {
      calCharge = true;
      var lien = lienCal || 'felix-morissette-cf6pjb/test-site';
      var calUrl = 'https://cal.com/' + lien;
      {% if customer %}
        calUrl += '?email={{ customer.email | url_encode }}';
        {% assign full_name = customer.first_name | append: ' ' | append: customer.last_name | strip %}
        {% if full_name != blank %}
          calUrl += '&name={{ full_name | url_encode }}';
        {% endif %}
      {% endif %}
      document.getElementById('lmm-cal-iframe').src = calUrl;
    }
  }
  function fermerCalModal() {
    document.getElementById('lmm-cal-modal').classList.remove('open');
  }
  function retourSelectionCouturiere() {
    document.getElementById('lmm-cal-modal').classList.remove('open');
    document.getElementById('lmm-localisation-modal').classList.add('open');
  }

  var rescheduleCharge = false;
  function ouvrirRescheduleModal() {
    fermerGestionModal();
    document.getElementById('lmm-reschedule-modal').classList.add('open');
    if (!rescheduleCharge) {
      rescheduleCharge = true;
      document.getElementById('lmm-reschedule-iframe').src = 'https://cal.com/reschedule/{{ customer.metafields.custom.rdv_uid }}';
    }
  }
  function fermerRescheduleModal() {
    document.getElementById('lmm-reschedule-modal').classList.remove('open');
    rescheduleCharge = false;
    document.getElementById('lmm-reschedule-iframe').src = '';
  }

  function ouvrirGestionModal() {
    document.getElementById('lmm-gestion-modal').classList.add('open');
  }

  function fermerGestionModal() {
    document.getElementById('lmm-gestion-modal').classList.remove('open');
  }

  function ouvrirAnnulationModal() {
    lmmAnnulationPourChangement = false;
    fermerGestionModal();
    document.getElementById('lmm-annulation-raison').value = '';
    document.getElementById('lmm-annulation-modal').classList.add('open');
  }

  var lmmAnnulationPourChangement = false;
  function ouvrirChangementCouturiere() {
    lmmAnnulationPourChangement = true;
    fermerGestionModal();
    document.getElementById('lmm-annulation-raison').value = 'Changement de tailleur';
    document.getElementById('lmm-annulation-modal').classList.add('open');
  }

  function fermerAnnulationModal() {
    document.getElementById('lmm-annulation-modal').classList.remove('open');
    document.getElementById('lmm-annulation-raison').value = '';
    document.getElementById('lmm-annulation-erreur').style.display = 'none';
  }

  function confirmerAnnulation() {
    var raison = document.getElementById('lmm-annulation-raison').value;
    var uid = '{{ customer.metafields.custom.rdv_uid }}';
    var pourChangement = lmmAnnulationPourChangement;

    // Afficher l'overlay immédiatement
    fermerAnnulationModal();
    var overlay = document.getElementById('lmm-booking-overlay');
    overlay.querySelector('p:first-child').textContent = 'Annulation en cours...';
    overlay.style.display = 'flex';

    fetch(tunnelUrl + '/api/cal-cancel', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ uid: uid, reason: raison })
    })
    .then(function(r) { return r.json(); })
    .then(function(data) {
      if (data.success) {
        if (pourChangement) {
          overlay.style.display = 'none';
          ouvrirLocalisationModal();
        } else {
          overlay.querySelector('p:first-child').textContent = 'Rendez-vous annulé';
          setTimeout(function() { window.location.reload(); }, 2000);
        }
      } else {
        overlay.style.display = 'none';
        document.getElementById('lmm-annulation-modal').classList.add('open');
        document.getElementById('lmm-annulation-erreur').style.display = 'block';
      }
    })
    .catch(function() {
      overlay.style.display = 'none';
      document.getElementById('lmm-annulation-modal').classList.add('open');
      document.getElementById('lmm-annulation-erreur').style.display = 'block';
    });
  }

  var emailRdv = '';

  window.addEventListener('message', function(e) {
    if (!e.data) return;
    var data = e.data.data || e.data;
    var type = e.data.type || (e.data.data && e.data.data.type) || '';

    // Capter l'email depuis les données Cal.com
    if (data && data.booking && data.booking.attendees && data.booking.attendees[0]) {
      emailRdv = data.booking.attendees[0].email || '';
    }
    if (data && data.email) emailRdv = data.email;

    if (type === 'bookingSuccessful' || type === '__cal__bookingSuccessful' || type === 'booking_successful' || String(type).toLowerCase().includes('booking')) {
      afficherConfirmation();
    }
  });

  document.getElementById('lmm-cal-iframe').addEventListener('load', function() {
    try {
      var iframeUrl = this.contentWindow.location.href;
      if (iframeUrl && (iframeUrl.includes('/booking/') || iframeUrl.includes('confirmed') || iframeUrl.includes('success'))) {
        afficherConfirmation();
      }
    } catch(e) {}
  });

  function afficherConfirmation() {
    fermerCalModal();
    {% unless customer %}
      // Sauvegarder le booking en localStorage pour synchronisation après connexion
      if (emailRdv) {
        document.getElementById('lmm-signup-email').value = emailRdv;
        document.getElementById('lmm-signup-link').href = '/account/login';
      }
      // Stocker les infos RDV pour les appliquer après connexion
      localStorage.setItem('lmm_rdv_pending', JSON.stringify({ email: emailRdv, ts: Date.now() }));
      document.getElementById('lmm-signup-modal').classList.add('open');
    {% else %}
      var overlay = document.getElementById('lmm-booking-overlay');
      overlay.querySelector('p:first-child').textContent = 'Rendez-vous confirmé';
      overlay.style.display = 'flex';

      // On attend la confirmation réelle de la synchronisation (au lieu d'un
      // délai fixe qui peut recharger la page avant que le webhook Cal.com
      // ait fini d'écrire les metafields) avant de rediriger. Un filet de
      // sécurité limite l'attente maximale pour ne jamais bloquer l'utilisateur.
      var syncTermine = fetch(tunnelUrl + '/api/cal-sync', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email: '{{ customer.email }}' })
      }).catch(function() {});

      var delaiMax = new Promise(function(resolve) { setTimeout(resolve, 6000); });

      Promise.race([syncTermine, delaiMax]).then(function() {
        window.location.href = '/cart';
      });
    {% endunless %}
  }
</script>

{% stylesheet %}
  .lmm-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: var(--lmm-bordeaux);
    border-bottom: 1px solid rgba(255,255,255,0.08);
    height: 104px;
  }

  .lmm-header__inner {
    max-width: 1520px;
    margin: 0 auto;
    padding: 0 32px;
    height: 100%;
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    align-items: center;
  }

  .lmm-header__cell {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  /* Séparateur discret entre chaque zone : une petite ligne verticale
     centrée (pas toute la hauteur du header), assez subtile pour
     apaiser l'œil sans attirer l'attention — et qui montre au passage
     que les 7 zones sont bien réparties également. */
  .lmm-header__cell + .lmm-header__cell::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 1px;
    height: 22px;
    background: rgba(var(--lmm-ivoire-deep-rgb), 0.18);
  }

  /* La règle ci-dessus ne met un trait qu'AVANT chaque zone (sauf la
     première) — la dernière zone (panier+compte) n'a donc jamais de
     séparateur après elle, à droite. Même trait, à droite cette fois. */
  .lmm-header__cell--icons::after {
    content: '';
    position: absolute;
    right: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 1px;
    height: 22px;
    background: rgba(var(--lmm-ivoire-deep-rgb), 0.18);
  }

  .lmm-header__logo {
    text-decoration: none;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .lmm-header__logo-img {
    display: block;
    width: auto;
  }

  .lmm-logo-main {
    font-family: Georgia, 'Times New Roman', serif;
    font-size: 26px;
    font-weight: 400;
    color: var(--lmm-ivoire-pale);
    letter-spacing: 0.04em;
  }

  .lmm-logo-sub {
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--lmm-bordeaux-pale);
    margin-top: 3px;
  }

  .lmm-nav-link {
    text-decoration: none;
    font-size: 12px;
    letter-spacing: 0.02em;
    color: var(--lmm-ivoire-pale);
    transition: color 0.2s;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  }

  .lmm-nav-link:hover,
  .lmm-nav-link--active {
    color: var(--lmm-ivoire-pale);
  }

  /* Même texte "Configurez votre complet" que les boutons pleins du hero
     et de la carte étape 3 — même couleur de police (--lmm-ivoire-pale)
     pour rester identique partout. */
  .lmm-nav-link--cta,
  .lmm-nav-link--cta:hover {
    color: var(--lmm-ivoire-pale);
  }

  .lmm-nav-link--cta:hover {
    opacity: 0.8;
  }

  /* Sous-menu "À propos" — s'ouvre au survol (desktop uniquement, voir
     le tiroir mobile pour l'équivalent tactile). La cellule doit couvrir
     toute la hauteur du header (contrairement aux autres cellules, qui
     s'ajustent à leur contenu) pour que le sous-menu, ancré à top:100%,
     s'aligne exactement sous le header plutôt qu'au milieu. */
  .lmm-header__cell--dropdown {
    height: 100%;
  }

  .lmm-header__dropdown {
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%) translateY(-6px);
    background: var(--lmm-bordeaux);
    border: 1px solid rgba(var(--lmm-ivoire-deep-rgb), 0.18);
    min-width: 200px;
    padding: 8px 0;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.18s ease, transform 0.18s ease;
    z-index: 50;
  }

  .lmm-header__cell--dropdown:hover .lmm-header__dropdown,
  .lmm-header__cell--dropdown:focus-within .lmm-header__dropdown {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
    pointer-events: auto;
  }

  .lmm-header__dropdown-link {
    display: block;
    padding: 12px 22px;
    font-size: 12px;
    letter-spacing: 0.02em;
    color: var(--lmm-ivoire-pale);
    text-decoration: none;
    white-space: nowrap;
    transition: background 0.15s ease;
  }

  .lmm-header__dropdown-link:hover {
    background: rgba(255, 255, 255, 0.06);
  }

  .lmm-header__icons {
    display: flex;
    align-items: center;
    gap: 18px;
  }

  .lmm-icon-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--lmm-ivoire-pale);
    text-decoration: none;
    transition: color 0.2s;
    position: relative;
  }

  .lmm-icon-btn:hover {
    color: var(--lmm-ivoire-pale);
  }

  .lmm-icon-btn svg {
    width: 19px;
    height: 19px;
  }

  .lmm-cart-count {
    position: absolute;
    top: -2px;
    right: -3px;
    background: var(--lmm-ivoire);
    width: 8px;
    height: 8px;
    border-radius: 50%;
    border: 1.5px solid var(--lmm-bordeaux);
  }

  .lmm-visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* Bouton hamburger — caché en desktop, seule la grille du header en
     dessous de 900px le fait apparaître (voir @media plus bas). Les 3
     barres se transforment en croix à l'ouverture. */
  .lmm-header__cell--toggle {
    display: none;
  }

  .lmm-menu-toggle {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 5px;
    width: 32px;
    height: 32px;
    background: none;
    border: none;
    padding: 0;
    cursor: pointer;
  }

  .lmm-menu-toggle__bar {
    display: block;
    width: 22px;
    height: 1.5px;
    background: var(--lmm-ivoire-pale);
    transition: transform 0.2s ease, opacity 0.2s ease;
  }

  .lmm-menu-toggle[aria-expanded="true"] .lmm-menu-toggle__bar:nth-child(1) {
    transform: translateY(6.5px) rotate(45deg);
  }

  .lmm-menu-toggle[aria-expanded="true"] .lmm-menu-toggle__bar:nth-child(2) {
    opacity: 0;
  }

  .lmm-menu-toggle[aria-expanded="true"] .lmm-menu-toggle__bar:nth-child(3) {
    transform: translateY(-6.5px) rotate(-45deg);
  }

  /* Tiroir du menu mobile — panneau plein-largeur ancré sous le header
     fixe (top: 104px, hauteur du header). Caché par défaut (opacity +
     pointer-events) plutôt que display:none pour permettre la
     transition d'ouverture. */
  .lmm-mobile-nav {
    position: fixed;
    top: 104px;
    left: 0;
    right: 0;
    background: var(--lmm-bordeaux);
    z-index: 99;
    display: flex;
    flex-direction: column;
    padding: 8px 24px 24px;
    max-height: calc(100dvh - 104px);
    overflow-y: auto;
    opacity: 0;
    transform: translateY(-8px);
    pointer-events: none;
    transition: opacity 0.2s ease, transform 0.2s ease;
  }

  .lmm-mobile-nav.open {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  .lmm-mobile-nav__link {
    text-decoration: none;
    font-size: 15px;
    letter-spacing: 0.02em;
    color: var(--lmm-ivoire-pale);
    padding: 16px 4px;
    border-bottom: 1px solid rgba(var(--lmm-ivoire-deep-rgb), 0.18);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  }

  .lmm-mobile-nav__link:last-child {
    border-bottom: none;
  }

  .lmm-mobile-nav__link--active {
    color: var(--lmm-ivoire);
  }

  /* Même couleur que les boutons pleins pour "Configurez votre complet" — voir .lmm-nav-link--cta. */
  .lmm-mobile-nav__link--cta {
    color: var(--lmm-ivoire-pale);
  }

  .lmm-mobile-nav__backdrop {
    display: none;
    position: fixed;
    top: 104px;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(var(--lmm-text-rgb), 0.45);
    z-index: 98;
  }

  .lmm-mobile-nav__backdrop.open {
    display: block;
  }

  /* En dessous de 900px, la grille à 7 colonnes du header déborde déjà
     (liens qui se compressent/chevauchent) — on bascule sur un hamburger
     plutôt que d'essayer de faire tenir 5 liens + logo + icônes sur une
     seule rangée. */
  @media (max-width: 900px) {
    .lmm-header__inner {
      grid-template-columns: 40px 1fr 76px;
      padding: 0 20px;
    }

    .lmm-header__cell--toggle {
      display: flex;
      justify-content: flex-start;
    }

    .lmm-header__cell--nav {
      display: none;
    }

    .lmm-header__cell + .lmm-header__cell::before,
    .lmm-header__cell--icons::after {
      display: none;
    }
  }

  body {
    padding-top: 104px;
    background: var(--lmm-ivoire-pale);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    color: var(--lmm-text);
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
  }

  .lmm-main {
    flex: 1 0 auto;
    display: flex;
    flex-direction: column;
  }

  .lmm-cal-modal {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(var(--lmm-text-rgb),0.7);
    z-index: 9999;
    align-items: center;
    justify-content: center;
  }

  .lmm-cal-modal.open {
    display: flex;
  }

  .lmm-cal-modal__inner {
    background: var(--lmm-ivoire-pale);
    width: 90%;
    max-width: 820px;
    max-height: 90vh;
    overflow-y: auto;
    position: relative;
  }

  .lmm-cal-modal__close {
    position: absolute;
    top: 12px;
    right: 16px;
    background: none;
    border: none;
    font-size: 24px;
    cursor: pointer;
    color: var(--lmm-muted);
    z-index: 1;
    line-height: 1;
  }

  .lmm-cal-modal__close:hover {
    color: var(--lmm-text);
  }

  #lmm-cal-embed {
    min-height: 600px;
  }

  .lmm-toggle-track {
    position: relative;
    display: inline-block;
    width: 30px;
    height: 17px;
    background: var(--lmm-border);
    border-radius: 0;
    flex-shrink: 0;
    transition: background 0.2s ease;
  }

  .lmm-toggle-thumb {
    position: absolute;
    top: 2px;
    left: 2px;
    width: 13px;
    height: 13px;
    background: var(--lmm-ivoire-pale);
    border-radius: 0;
    box-shadow: 0 1px 2px rgba(0,0,0,0.25);
    transition: transform 0.2s ease;
  }

  #lmm-vendeuse-toggle.active .lmm-toggle-track {
    background: var(--lmm-bordeaux);
  }

  #lmm-vendeuse-toggle.active .lmm-toggle-thumb {
    transform: translateX(13px);
  }

  @keyframes lmm-progress {
    from { width: 0px; }
    to { width: 200px; }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "En-tête",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo_image",
      "label": "Logo (laisser vide pour afficher le texte)"
    },
    {
      "type": "range",
      "id": "logo_height",
      "label": "Hauteur du logo",
      "min": 20,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 32
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu principal",
      "default": "main-menu"
    },
    {
      "type": "link_list",
      "id": "customer_account_menu",
      "label": "Menu compte client",
      "default": "customer-account-main-menu"
    },
    {
      "type": "text",
      "id": "backend_url",
      "label": "URL du backend",
      "default": "http://localhost:3000"
    },
    {
      "type": "textarea",
      "id": "horaire_couturieres",
      "label": "Horaire habituel des couturières",
      "info": "Affiché si un client refuse de créer un compte pour un rdv « tissus ». À CONFIRMER — valeur de départ à vérifier/remplacer, doit correspondre à la disponibilité réelle sur Cal.com.",
      "default": "Lundi au vendredi, 10 h à 18 h\nSamedi, 10 h à 16 h"
    }
  ]
}
{% endschema %}
